using System; using System.Reflection; using Whisper.net.Ggml; class P { static void Main() { foreach (var p in typeof(WhisperGgmlDownloader).GetProperties(BindingFlags.Public | BindingFlags.Static)) Console.WriteLine("Prop: " + p.Name); foreach (var m in typeof(WhisperGgmlDownloader).GetMethods(BindingFlags.Public | BindingFlags.Static)) Console.WriteLine("Method: " + m.Name); } }
